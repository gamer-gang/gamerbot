import { CommandResult } from '../../command.js'
import { CommandContext } from '../../context.js'

export const truthOrDareKick = async (context: CommandContext): Promise<CommandResult> => {
  return CommandResult.Success
}
